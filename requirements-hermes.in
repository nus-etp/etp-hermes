# Extra packages installed into hermes-agent's venv on CI (not this repo's
# venv — see the "Install ddgs + langfuse into hermes venv" step in
# .github/workflows/hermes-sync.yml for why each is needed). Also reused by
# the nightly LLM eval job (evals.yml) as its hash-pinned langfuse source.
#
# Compiled to requirements-hermes.txt with hashes via `make pin-hermes-extras`.
ddgs>=9.14.4
langfuse>=4.7.1
